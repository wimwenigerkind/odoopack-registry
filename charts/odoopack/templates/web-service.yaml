apiVersion: v1
kind: Service
metadata:
  name: {{ include "odoopack.fullname" . }}-web
  labels:
    {{- include "odoopack.labels" . | nindent 4 }}
    app.kubernetes.io/component: web
spec:
  type: {{ .Values.web.service.type }}
  ports:
    - port: {{ .Values.web.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "odoopack.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: web
