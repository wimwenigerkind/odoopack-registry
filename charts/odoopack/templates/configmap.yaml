apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "odoopack.fullname" . }}-config
  labels:
    {{- include "odoopack.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- $cfg := mergeOverwrite (deepCopy .Values.config) .Values.extraConfig }}
    {{- toYaml $cfg | nindent 4 }}
