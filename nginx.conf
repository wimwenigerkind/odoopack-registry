server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location ^~ /api/ {
        add_header Cache-Control "no-store" always;
        return 404;
    }

    location / {
        try_files $uri /index.html;
    }
}